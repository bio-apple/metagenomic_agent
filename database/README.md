# Reference databases

Place indexes under this tree or point `config/default.yaml` `paths.*` to absolute locations:

```
database/
├── kraken_db/
├── gtdb/
└── eggnog/
```

`mock` / empty dirs automatically fall back to simulated outputs. No private Docker images required.
